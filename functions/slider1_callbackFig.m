%% Callback subfunctions to support UI actions for single image
function slider1_callbackFig(~,~,vars_b, f, dire)
    % Run slider1 which controls value of epsilon
    plotterfcnFig(vars_b, f, dire)
end